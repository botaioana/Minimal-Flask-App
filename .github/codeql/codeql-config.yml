name: "Python Security Configuration"
disable-default-queries: true  # Moved here from workflow

queries:
  - uses: security-and-quality

paths-ignore:
  - '**/tests/**'
  - '**/docs/**'
  - '**/venv/**'
