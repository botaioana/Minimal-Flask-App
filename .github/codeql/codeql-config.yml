name: "Custom Python Security Scan"
disable-default-queries: true

queries:
  - uses: security-extended
  - uses: security-experimental
  - name: Custom Flask debug query
    uses: ./.github/codeql-queries/FlaskDebug.ql  # Dacă ai query-uri custom

paths-ignore:
  - '**/tests/**'
  - '**/node_modules/**'
  - '**/venv/**'

languages:
  - python
